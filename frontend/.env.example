# Base URL of the review API. Leave empty to use the same origin
# (the Vite dev server proxies /review to VITE_PROXY_TARGET by default).
VITE_API_BASE_URL=

# Dev-only: where to proxy /review requests during `npm run dev`.
# Point this at the running backend API server.
VITE_PROXY_TARGET=http://localhost:8000